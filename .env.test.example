# Copy to .env.test (gitignored) and fill in real values for local test runs.
# Used by tests/auth.setup.ts to authenticate the Playwright test account.
FABRIOS_TEST_EMAIL=you@example.com
FABRIOS_TEST_PASSWORD=changeme
